import { Context, ContextValue } from './utils';
export declare function createContextManager(): {
    get(): Context;
    add(key: string, value: ContextValue): void;
    remove(key: string): void;
    set(newContext: Context): void;
};
